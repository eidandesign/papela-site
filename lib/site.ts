// Canonical site URL — single source of truth for absolute URLs.
// Never derive payment redirect URLs from the request `Origin` header (client-controlled).
export const SITE_URL = "https://www.papela-atelier.com";
